<div class="a_banner"></div>
<div class="newtab">
    <div id="ib-main-wrapper" class="ib-main-wrapper">
        <div class="ib-main"><span class="idTabs"><a href="#" rel="#locA" class="locOn"> 关于方分</a><a href="#"
                                                                                                    rel="#locB">组织结构</a><a
                href="#" rel="#locC">服务承诺</a><a href="#" rel="#locD"> 办公环境</a><a href="#" rel="#locE">荣誉室大事记 </a></span>

            <div class="clr"></div>
        </div>
    </div>
    <div class="row" style="padding-bottom:50px;"><!--1-->
        <ul id="locA" class="loc">
            <div class="my_left">
                <h2>公司简介 </h2>
                <h3><img src="${ctx}/static/images/about1.jpg" tppabs="${ctx}/static/images/about1.jpg"></h3>
            </div>
            <div class="my_right"> 　　　
                <p> 厦门市方分拍卖有限公司是经厦门市商务局批准，2014年于厦门市工商局注册成立的拍卖企业。公司注册资金1000万元，公司拥有自有产权办公场所面积1631平方米，
                    其中设有500平方米的多功能拍卖大厅，能同时容纳200多人参加拍卖，拍卖大厅设施齐全，配备专业投影、音响和闭路监控系统等现代化办公设备.另外公司还有近1000平方米的专业艺术品展厅，经常性举办书画及其它艺术品展览。</p> 　　
                <p> 公司下设拍卖管理部、资产业务部、艺术品部、综合部，同时，在北京、上海、西安、南京等地均派驻业务代表。公司拥有一支专业性强、综合素质高、朝气蓬勃的实力队伍，拥有国家注册拍卖师2名、律师1名、注册会计师2名、
                    艺术顾问2名在内的高素质人才，公司现有人员均为大专以上学历，经过福建省拍卖行业协会培训，具有《拍卖从业人员资格证书》。</p><br/>
                <p> 厦门市方分拍卖有限公司以艺术品拍卖为重心，拥有独特的优势和资源，关联企业有——金凯龙艺术馆、 《台港文学选刊》杂志社（国内统一刊号：CN 35-1069/1）、《两岸艺术》杂志社（厦门市金凯龙印刷有限公司与厦门市美术家协会合办）、
                    厦门市金凯龙印刷有限公司。</p><br/>
                <p> 公司业务以艺术品拍卖为核心，同时承接法律、法规允许拍卖的各种物品和财产权利；包括个人合法的产业和物品变现，人民法院强制执行的标的、充抵押金、罚款的标的，抵押银行贷款的标的，破产企业的标的，党政机关、
                    企事业单位需要处理的标的，非物化产权、专利、发明和编码等。</p><br/>
                <p> 厦门市方分拍卖有限公司严格遵守《中华人民共和国拍卖法》及有关法律、法规，坚持“公开、公平、公正、诚实信用”原则，竭诚为广大客户提供优质高效服务。</p>
                　　

            </div>
        </ul>
        <!--2-->
        <ul id="locB" class="loc">
            <div class="zuzhi">
                <h2>组织结构</h2>
                <img src="${ctx}/static/images/zuzhi.jpg" tppabs="${ctx}/static/images/zuzhi.jpg">
            </div>
        </ul>
        <!--3-->
        <ul id="locC" class="loc">
            <div class="my_left">
                <h2>服务承诺</h2>

                <h3><img src="${ctx}/static/images/about4.jpg" tppabs="${ctx}/static/images/about4.jpg"></h3>
            </div>
            <div class="my_right"> 　
                <p style=" margin-top:46px; font-size:14px; color:#333; line-height:24px;">
                    我公司将一直恪守"诚信、稳健、自律、发展"的宗旨，坚持"公开、公平、公正、诚实信用"的经营方针，以现代企业制度规范公司运作和经营行为，为各方当事人提供完善、专业、高效的拍卖服务。 </p>
            </div>
        </ul>
        <!--4-->
        <ul id="locD" class="loc">
            <div class="official">
                <h2>办公环境</h2>
                <img src="${ctx}/static/images/b1.jpg" tppabs="${ctx}/static/images/b1.jpg">
                <img src="${ctx}/static/images/b2.jpg" tppabs="${ctx}/static/images/b2.jpg">
                <img src="${ctx}/static/images/b3.jpg" tppabs="${ctx}/static/images/b3.jpg">
                <img src="${ctx}/static/images/b4.jpg" tppabs="${ctx}/static/images/b4.jpg">
                <img src="${ctx}/static/images/b5.jpg" tppabs="${ctx}/static/images/b5.jpg">
                <img src="${ctx}/static/images/b6.jpg" tppabs="${ctx}/static/images/b6.jpg">
            </div>
        </ul>
        <!--5-->
        <ul id="locE" class="loc">
            <div class="honour">
                <h2>荣誉室大事记 </h2>
                <span class="s_1"><img src="${ctx}/static/images/1.jpg" tppabs="${ctx}/static/images/1.jpg"></span><span
                    class="s_1"><img src="${ctx}/static/images/2.jpg" tppabs="${ctx}/static/images/2.jpg"></span><span
                    class="s_1"><img src="${ctx}/static/images/3.jpg" tppabs="${ctx}/static/images/3.jpg"></span><img
                    src="${ctx}/static/images/4.JPG" tppabs="${ctx}/static/images/4.JPG"><img
                    src="${ctx}/static/images/5.JPG" tppabs="${ctx}/static/images/5.JPG"><span class="s_1"><img
                    src="${ctx}/static/images/6.jpg" tppabs="${ctx}/static/images/6.jpg"></span><span class="s_1"><img
                    src="${ctx}/static/images/7.jpg" tppabs="${ctx}/static/images/7.jpg"></span><span class="s_1"><img
                    src="${ctx}/static/images/8.jpg" tppabs="${ctx}/static/images/8.jpg"></span><img
                    src="${ctx}/static/images/9.jpg" tppabs="${ctx}/static/images/9.jpg"><img
                    src="${ctx}/static/images/10.jpg" tppabs="${ctx}/static/images/10.jpg"><img
                    src="${ctx}/static/images/11.JPG" tppabs="${ctx}/static/images/11.JPG"><img
                    src="${ctx}/static/images/12.JPG" tppabs="${ctx}/static/images/12.JPG"><img
                    src="${ctx}/static/images/13.JPG" tppabs="${ctx}/static/images/13.JPG"><img
                    src="${ctx}/static/images/14.JPG" tppabs="${ctx}/static/images/14.JPG"></div>
        </ul>
    </div>
</div>


<!-- end footer --></div>
<!-- end layout --></div>
<!-- end frame -->
<div id="toTop"><i class="icon-angle-up"></i></div>
<!-- Back to top --><!-- 公共Scripts -->
<script type="text/javascript" src="${ctx}/static/js/jquery.min.js" tppabs="${ctx}/static/js/jquery.min.js"></script>
<script type="text/javascript" src="${ctx}/static/js/zzsc.js" tppabs="${ctx}/static/js/zzsc.js"></script><!--滚动图片-->
<script type="text/javascript" src="${ctx}/static/js/rs-plugin/js/jquery.themepunch.revolution.min.js"
        tppabs="${ctx}/static/js/rs-plugin/js/jquery.themepunch.revolution.min.js"></script>
<script type="text/javascript" src="${ctx}/static/js/theme20.js" tppabs="${ctx}/static/js/theme20.js"></script>
<script type="text/javascript" src="${ctx}/static/js/jquery.nicescroll.min.js"
        tppabs="${ctx}/static/js/jquery.nicescroll.min.js"></script>
<script type="text/javascript" src="${ctx}/static/js/custom.js" tppabs="${ctx}/static/js/custom.js"></script>
<script type="text/javascript">    /* <![CDATA[ */
var tpj = jQuery;
tpj.noConflict();
tpj(document).ready(function () {
    if (tpj.fn.cssOriginal != undefined)
        tpj.fn.css = tpj.fn.cssOriginal;
    var api = tpj('.revolution').revolution({
        delay: 6000,
        startheight: 410,
        startwidth: 1140,
        navigationType: "bullet",
        navigationArrows: "solo",
        navigationStyle: "round",
        touchenabled: "on",
        onHoverStop: "on",
        navOffsetHorizontal: 0,
        navOffsetVertical: 20,
        shadow: 0
    });
});
/* ]]> */
</script><!-- 公共Scripts --><!--滚动-->
<script type="text/javascript" src="${ctx}/static/js/jquery.kinetic.js"
        tppabs="${ctx}/static/js/jquery.kinetic.js"></script>
<script type="text/javascript">        (function ($) {
    $(function () {

        var $ibWrapper = $('#ib-main-wrapper'),

                Template = (function () {

                    // true if dragging the container
                    var kinetic_moving = false,
                    // current index of the opened item
                            current = -1,
                    // true if the item is being opened / closed
                            isAnimating = false,
                    // items on the grid
                            $ibItems = $ibWrapper.find('div.ib-main > a'),
                    // image items on the grid
                            $ibImgItems = $ibItems.not('.ib-content'),
                    // total image items on the grid
                            imgItemsCount = $ibImgItems.length,
                            init = function () {

                                // add a class ib-image to the image items
                                $ibImgItems.addClass('ib-image');
                                // apply the kinetic plugin to the wrapper
                                loadKinetic();
                                // load some events
                                initEvents();

                            },
                            loadKinetic = function () {

                                setWrapperSize();

                                $ibWrapper.kinetic({
                                    moved: function () {

                                        kinetic_moving = true;

                                    },
                                    stopped: function () {

                                        kinetic_moving = false;

                                    }
                                });

                            },
                            setWrapperSize = function () {

                                var containerMargins = $('#ib-top').outerHeight(true) + $('#header').outerHeight(true) + parseFloat($ibItems.css('margin-top'));
                                $ibWrapper.css('height', $(window).height() - containerMargins)

                            },
                            initEvents = function () {

                                // open the item only if not dragging the container
                                $ibItems.bind('click.ibTemplate', function (event) {

                                    if (!kinetic_moving)
                                        openItem($(this));

                                    return false;

                                });

                                // on window resize, set the wrapper and preview size accordingly
                                $(window).bind('resize.ibTemplate', function (event) {

                                    setWrapperSize();

                                    $('#ib-img-preview, #ib-content-preview').css({
                                        width: $(window).width(),
                                        height: $(window).height()
                                    })

                                });

                            },
                            openItem = function ($item) {

                                if (isAnimating) return false;

                                // if content item
                                if ($item.hasClass('ib-content')) {

                                    isAnimating = true;
                                    current = $item.index('.ib-content');
                                    loadContentItem($item, function () {
                                        isAnimating = false;
                                    });

                                }
                                // if image item
                                else {

                                    isAnimating = true;
                                    current = $item.index('.ib-image');
                                    loadImgPreview($item, function () {
                                        isAnimating = false;
                                    });

                                }

                            },
                    // opens one image item (fullscreen)
                            loadImgPreview = function ($item, callback) {

                                var largeSrc = $item.children('img').data('largesrc'),
                                        description = $item.children('span').text(),
                                        largeImageData = {
                                            src: largeSrc,
                                            description: description
                                        };

                                // preload large image
                                $item.addClass('ib-loading');

                                preloadImage(largeSrc, function () {

                                    $item.removeClass('ib-loading');

                                    var hasImgPreview = ( $('#ib-img-preview').length > 0 );

                                    if (!hasImgPreview)
                                        $('#previewTmpl').tmpl(largeImageData).insertAfter($ibWrapper);
                                    else
                                        $('#ib-img-preview').children('img.ib-preview-img')
                                                .attr('src', largeSrc)
                                                .end()
                                                .find('span.ib-preview-descr')
                                                .text(description);

                                    //get dimentions for the image, based on the windows size
                                    var dim = getImageDim(largeSrc);

                                    $item.removeClass('ib-img-loading');

                                    //set the returned values and show/animate preview
                                    $('#ib-img-preview').css({
                                        width: $item.width(),
                                        height: $item.height(),
                                        left: $item.offset().left,
                                        top: $item.offset().top
                                    }).children('img.ib-preview-img').hide().css({
                                        width: dim.width,
                                        height: dim.height,
                                        left: dim.left,
                                        top: dim.top
                                    }).fadeIn(400).end().show().animate({
                                        width: $(window).width(),
                                        left: 0
                                    }, 500, 'easeOutExpo', function () {

                                        $(this).animate({
                                            height: $(window).height(),
                                            top: 0
                                        }, 400, function () {

                                            var $this = $(this);
                                            $this.find('span.ib-preview-descr, span.ib-close').show()
                                            if (imgItemsCount > 1)
                                                $this.find('div.ib-nav').show();

                                            if (callback) callback.call();

                                        });

                                    });

                                    if (!hasImgPreview)
                                        initImgPreviewEvents();

                                });

                            },
                    // opens one content item (fullscreen)
                            loadContentItem = function ($item, callback) {

                                var hasContentPreview = ( $('#ib-content-preview').length > 0 ),
                                        teaser = $item.children('div.ib-teaser').html(),
                                        content = $item.children('div.ib-content-full').html(),
                                        contentData = {
                                            teaser: teaser,
                                            content: content
                                        };

                                if (!hasContentPreview)
                                    $('#contentTmpl').tmpl(contentData).insertAfter($ibWrapper);

                                //set the returned values and show/animate preview
                                $('#ib-content-preview').css({
                                    width: $item.width(),
                                    height: $item.height(),
                                    left: $item.offset().left,
                                    top: $item.offset().top
                                }).show().animate({
                                    width: $(window).width(),
                                    left: 0
                                }, 500, 'easeOutExpo', function () {

                                    $(this).animate({
                                        height: $(window).height(),
                                        top: 0
                                    }, 400, function () {

                                        var $this = $(this),
                                                $teaser = $this.find('div.ib-teaser'),
                                                $content = $this.find('div.ib-content-full'),
                                                $close = $this.find('span.ib-close');

                                        if (hasContentPreview) {
                                            $teaser.html(teaser)
                                            $content.html(content)
                                        }

                                        $teaser.show();
                                        $content.show();
                                        $close.show();

                                        if (callback) callback.call();

                                    });

                                });

                                if (!hasContentPreview)
                                    initContentPreviewEvents();

                            },
                    // preloads an image
                            preloadImage = function (src, callback) {

                                $('<img/>').load(function () {

                                    if (callback) callback.call();

                                }).attr('src', src);

                            },
                    // load the events for the image preview : navigation ,close button, and window resize
                            initImgPreviewEvents = function () {

                                var $preview = $('#ib-img-preview');

                                $preview.find('span.ib-nav-prev').bind('click.ibTemplate', function (event) {

                                    navigate('prev');

                                }).end().find('span.ib-nav-next').bind('click.ibTemplate', function (event) {

                                    navigate('next');

                                }).end().find('span.ib-close').bind('click.ibTemplate', function (event) {

                                    closeImgPreview();

                                });

                                //resizing the window resizes the preview image
                                $(window).bind('resize.ibTemplate', function (event) {

                                    var $largeImg = $preview.children('img.ib-preview-img'),
                                            dim = getImageDim($largeImg.attr('src'));

                                    $largeImg.css({
                                        width: dim.width,
                                        height: dim.height,
                                        left: dim.left,
                                        top: dim.top
                                    })

                                });

                            },
                    // load the events for the content preview : close button
                            initContentPreviewEvents = function () {

                                $('#ib-content-preview').find('span.ib-close').bind('click.ibTemplate', function (event) {

                                    closeContentPreview();

                                });

                            },
                    // navigate the image items in fullscreen mode
                            navigate = function (dir) {

                                if (isAnimating) return false;

                                isAnimating = true;

                                var $preview = $('#ib-img-preview'),
                                        $loading = $preview.find('div.ib-loading-large');

                                $loading.show();

                                if (dir === 'next') {

                                    ( current === imgItemsCount - 1 ) ? current = 0 : ++current;

                                }
                                else if (dir === 'prev') {

                                    ( current === 0 ) ? current = imgItemsCount - 1 : --current;

                                }

                                var $item = $ibImgItems.eq(current),
                                        largeSrc = $item.children('img').data('largesrc'),
                                        description = $item.children('span').text();

                                preloadImage(largeSrc, function () {

                                    $loading.hide();

                                    //get dimentions for the image, based on the windows size
                                    var dim = getImageDim(largeSrc);

                                    $preview.children('img.ib-preview-img')
                                            .attr('src', largeSrc)
                                            .css({
                                                width: dim.width,
                                                height: dim.height,
                                                left: dim.left,
                                                top: dim.top
                                            })
                                            .end()
                                            .find('span.ib-preview-descr')
                                            .text(description);

                                    $ibWrapper.scrollTop($item.offset().top)
                                            .scrollLeft($item.offset().left);

                                    isAnimating = false;

                                });

                            },
                    // closes the fullscreen image item
                            closeImgPreview = function () {

                                if (isAnimating) return false;

                                isAnimating = true;

                                var $item = $ibImgItems.eq(current);

                                $('#ib-img-preview').find('span.ib-preview-descr, div.ib-nav, span.ib-close')
                                        .hide()
                                        .end()
                                        .animate({
                                            height: $item.height(),
                                            top: $item.offset().top
                                        }, 500, 'easeOutExpo', function () {

                                            $(this).animate({
                                                width: $item.width(),
                                                left: $item.offset().left
                                            }, 400, function () {

                                                $(this).fadeOut(function () {
                                                    isAnimating = false;
                                                });

                                            });

                                        });

                            },
                    // closes the fullscreen content item
                            closeContentPreview = function () {

                                if (isAnimating) return false;

                                isAnimating = true;

                                var $item = $ibItems.not('.ib-image').eq(current);

                                $('#ib-content-preview').find('div.ib-teaser, div.ib-content-full, span.ib-close')
                                        .hide()
                                        .end()
                                        .animate({
                                            height: $item.height(),
                                            top: $item.offset().top
                                        }, 500, 'easeOutExpo', function () {

                                            $(this).animate({
                                                width: $item.width(),
                                                left: $item.offset().left
                                            }, 400, function () {

                                                $(this).fadeOut(function () {
                                                    isAnimating = false;
                                                });

                                            });

                                        });

                            },
                    // get the size of one image to make it full size and centered
                            getImageDim = function (src) {

                                var img = new Image();
                                img.src = src;

                                var w_w = $(window).width(),
                                        w_h = $(window).height(),
                                        r_w = w_h / w_w,
                                        i_w = img.width,
                                        i_h = img.height,
                                        r_i = i_h / i_w,
                                        new_w, new_h,
                                        new_left, new_top;

                                if (r_w > r_i) {

                                    new_h = w_h;
                                    new_w = w_h / r_i;

                                }
                                else {

                                    new_h = w_w * r_i;
                                    new_w = w_w;

                                }

                                return {
                                    width: new_w,
                                    height: new_h,
                                    left: (w_w - new_w) / 2,
                                    top: (w_h - new_h) / 2
                                };

                            };

                    return { init: init };

                })();

        Template.init();

    });
})(jQuery)
</script><!--滚动-->
<script type="text/javascript" src="${ctx}/static/js/jquery.idTabs.min.js"
        tppabs="${ctx}/static/js/jquery.idTabs.min.js"></script>